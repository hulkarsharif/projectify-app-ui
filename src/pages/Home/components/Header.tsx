import styled from "styled-components";
import background from "../Images/bg-effect2.png";

const Header = styled.div`
    background: url(${background}) center/cover,
        linear-gradient(to bottom, #f7f3ff 0%, #fdfcff 100%);
`;

export { Header };
